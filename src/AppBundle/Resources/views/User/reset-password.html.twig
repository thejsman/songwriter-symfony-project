{% if changed is defined %}
    password was changed
{% else %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Change password">
    {{ form_end(form) }}
{% endif %}